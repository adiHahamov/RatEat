<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:scrollbars="vertical">

    <ImageView
        android:id="@+id/image_my_list_row"
        android:layout_width="330dp"
        android:layout_height="250dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="32dp"
        android:foregroundGravity="center_vertical"
        android:src="@drawable/pizza"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.47"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/editBtn"
        android:layout_width="73dp"
        android:layout_height="67dp"
        android:background="#FFBFB1B1"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="@+id/image_my_list_row"
        app:layout_constraintEnd_toEndOf="@+id/image_my_list_row"
        app:srcCompat="@drawable/ic_edit" />

    <ImageButton
        android:id="@+id/delBtn"
        android:layout_width="73dp"
        android:layout_height="67dp"
        android:background="#BFB1B1"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="@+id/image_my_list_row"
        app:layout_constraintStart_toStartOf="@+id/image_my_list_row"
        app:srcCompat="@drawable/ic_delete" />

</androidx.constraintlayout.widget.ConstraintLayout>